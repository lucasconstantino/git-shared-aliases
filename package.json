{
  "name": "git-local-aliases",
  "version": "0.1.0",
  "description": "A drop-in tool for easy local shared Git aliases",
  "license": "MIT",
  "private": false,
  "scripts": {
    "postinstall": "node ./install-aliases.js"
  }
}
